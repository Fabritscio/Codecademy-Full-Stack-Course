// Write your function here:
const howOld = (age, year) => {
  
    date.time = year
    if (year === 'future') {
      return `You will be ${age} in the year ${year}`
    } else if (year === 'before') {
      return `The year ${year} was ${year} years before you were born`;
    } else if (year === 'past') {
      return `The year ${age} in the year ${year}`;
    }
  }
  